<ui:FluentWindow
    x:Class="Hurl.RulesetManager.Windows.RuleTestingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Hurl.RulesetManager.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Rule testing suite"
    Width="500"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="NoResize"
    SizeToContent="Height"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Title="Rule testing suite"
            ShowMaximize="False"
            ShowMinimize="False" />

        <TextBlock
            Grid.Row="1"
            Margin="16,8"
            FontSize="20"
            FontWeight="SemiBold"
            Text="Test a URI" />

        <ui:Card Grid.Row="2" Margin="16,0,16,8">
            <TextBox x:Name="_UriInput" HorizontalAlignment="Stretch" />
        </ui:Card>

        <ui:Card Grid.Row="3" Margin="16,0,16,8">
            <StackPanel>
                <CheckBox x:Name="UseCustomRule" Content="Test this URI using a custom rule" />

                <Grid Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="240" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Margin="0,0,0,8"
                        VerticalAlignment="Center"
                        Text="Rule Type" />
                    <ComboBox
                        x:Name="_RuleTypeInput"
                        Grid.Column="1"
                        MinWidth="120"
                        Margin="0,0,0,8"
                        HorizontalAlignment="Right"
                        IsEnabled="{Binding UseCustomRule.IsChecked}">
                        <ComboBoxItem Content="Regex" />
                        <ComboBoxItem Content="Glob" />
                        <ComboBoxItem Content="Domain" />
                        <ComboBoxItem Content="String" />
                    </ComboBox>

                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        Text="Rule" />
                    <TextBox
                        x:Name="_RuleInput"
                        Grid.Row="1"
                        Grid.Column="1"
                        HorizontalAlignment="Stretch"
                        AcceptsReturn="False"
                        IsEnabled="{Binding UseCustomRule.IsChecked}" />
                </Grid>
            </StackPanel>
        </ui:Card>

        <ui:Button
            x:Name="_TestButton"
            Grid.Row="4"
            Margin="16,0,16,8"
            HorizontalAlignment="Right"
            Appearance="Primary"
            Click="TestRuleButton_Click"
            Content="Test" />

        <ui:InfoBar
            x:Name="_errorCard"
            Title="Error"
            Grid.Row="5"
            Margin="16,0,16,8"
            Severity="Error" />
        <ui:InfoBar
            x:Name="_outputCard"
            Title="Result"
            Grid.Row="5"
            Margin="16,0,16,8"
            Severity="Informational" />
    </Grid>
</ui:FluentWindow>
