<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Hurl.Selector.Pages.SelectorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Hurl.Selector.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Hurl.Selector.Models"
    x:Name="SelectorRoot"
    Title="Hurl - Select"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="8" RowSpacing="8">
        <Grid.KeyboardAccelerators>
            <KeyboardAccelerator Invoked="EscapeAccelerator_Invoked" Key="Escape" />
            <KeyboardAccelerator Invoked="CopyAccelerator_Invoked" Key="C" />
            <KeyboardAccelerator Invoked="EditUrlAccelerator_Invoked" Key="E" />
            <KeyboardAccelerator Invoked="RulesAccelerator_Invoked" Key="R" />
        </Grid.KeyboardAccelerators>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            VerticalAlignment="Center"
            Style="{StaticResource BodyTextBlockStyle}"
            Text="Select a browser" />

        <ItemsRepeater
            Grid.Row="1"
            VerticalAlignment="Center"
            ItemsSource="{x:Bind ViewModel.Browsers, Mode=OneWay}">
            <ItemsRepeater.Layout>
                <UniformGridLayout
                    ItemsJustification="Center"
                    MaximumRowsOrColumns="4"
                    MinColumnSpacing="12"
                    MinItemHeight="132"
                    MinItemWidth="132"
                    MinRowSpacing="8" />
            </ItemsRepeater.Layout>

            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="models:Browser">
                    <Grid>
                        <Button
                            Padding="4"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Command="{Binding ViewModel.LaunchBrowserCommand, ElementName=SelectorRoot}"
                            CommandParameter="{x:Bind}"
                            Loaded="BrowserButton_Loaded"
                            ToolTipService.ToolTip="{x:Bind Name}"
                            Tag="{x:Bind}">
                            <Grid RowSpacing="6">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="80" />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <Image Source="{x:Bind Icon, Mode=OneWay}" />
                                <TextBlock
                                    Grid.Row="1"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="{x:Bind Name, Mode=OneWay}"
                                    TextAlignment="Center"
                                    TextWrapping="NoWrap" />
                            </Grid>
                        </Button>
                        <Button
                            x:Name="AdditionalBtn"
                            Margin="2"
                            Padding="2"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Background="{x:Null}"
                            BorderBrush="{x:Null}"
                            Click="AdditionalBtn_Click"
                            Loaded="AdditionalBtn_Loaded"
                            Tag="{x:Bind}"
                            Visibility="{x:Bind ShowAdditionalBtn, Mode=OneWay}">
                            <Button.Content>
                                <FontIcon Glyph="&#xE712;" />
                            </Button.Content>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>

        <Border
            Grid.Row="2"
            Padding="4"
            HorizontalAlignment="Center"
            BorderBrush="{StaticResource ControlElevationBorderBrush}"
            BorderThickness="1.5"
            CornerRadius="6">
            <Grid ColumnSpacing="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition MinWidth="240" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button
                    Padding="6"
                    Background="{x:Null}"
                    BorderBrush="{x:Null}"
                    ToolTipService.ToolTip="Copy URL (C)"
                    Click="LinkCopyBtnClick">
                    <Button.Content>
                        <FontIcon FontSize="16" Glyph="&#xE8C8;" />
                    </Button.Content>
                </Button>
                <TextBox
                    x:Name="UrlTextBox"
                    Grid.Column="1"
                    MaxHeight="32"
                    Padding="6"
                    HorizontalAlignment="Stretch"
                    BorderBrush="{StaticResource ControlElevationBorderBrush}"
                    FontSize="12"
                    IsSpellCheckEnabled="False"
                    IsTextPredictionEnabled="False"
                    ToolTipService.ToolTip="Edit URL (E)"
                    PlaceholderText="URL"
                    Text="{x:Bind ViewModel.Url, Mode=TwoWay}" />
                <AppBarSeparator Grid.Column="2" />
                <DropDownButton Grid.Column="3" Height="32">
                    <DropDownButton.Content>
                        <FontIcon FontSize="16" Glyph="&#xE700;" />
                    </DropDownButton.Content>
                    <DropDownButton.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem
                                Click="Button_Click_1"
                                Tag="settings"
                                Text="Rules">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE710;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem
                                Click="SettingsBtnClick"
                                Tag="exit"
                                Text="Settings">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE713;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSeparator />
                            <MenuFlyoutItem
                                Click="Button_Click"
                                Tag="reload"
                                Text="Timed">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE916;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
            </Grid>
        </Border>
    </Grid>
</Window>
